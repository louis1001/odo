var e = 2.718281828459045

func exp(x: double): double {
    return e ^ x
}

func abs(x: double): double {
    if x > 0 {
        return x
    }

    return -x
}

func pow(x: double, p: int): double {
    var result = 1.0
    forange _ ~: p {
        result = result * x
    }

    return result
}