var greetings_func = func (name: string, messages: int, temp: double) {
    writeln(
        "Hello, ", name, ". You have ", messages,
        " new messages. The temparature is ", temp,
        " degrees."
    )
}

var i = read_double("Write a double: ")

var j = read_int("Write a int: ")

var name = read("Write your name: ")

clear()
greetings_func(name, j, i)

wait()
