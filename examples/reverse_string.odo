
writeln("Testing reversing a string")

string first = "A palindrome is the ideal test for this file" # read()

string result = ""
forange i ~: length(first) {
    result += first[i]
}

writeln(result)